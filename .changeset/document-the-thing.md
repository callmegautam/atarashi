---
'atarashi': patch
---

Rewrite the documentation for v1: a new README, a user-facing migration guide,
and a guide covering getting started, configuration, recipes, troubleshooting,
authoring blueprints and the plugin API. The CLI reference, the blueprint
catalogue, the preset pages and every blueprint's own README are generated by
`pnpm build:docs` from the binary's help output, the manifests and real
generation plans, and `pnpm check:docs` fails the release gate when they drift.
