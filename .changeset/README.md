# Changesets

This folder holds [changesets](https://github.com/changesets/changesets). Each
package in this monorepo is versioned independently; run `pnpm changeset` to
record a change, and the release workflow turns accumulated changesets into a
"Version Packages" PR.
