export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-950 px-6 text-slate-100">
      <section className="max-w-xl space-y-4 text-center">
        <h1 className="text-4xl font-semibold tracking-tight">{{ project.name }}</h1>
        <p className="text-slate-400">
          React, Vite and Tailwind are wired up. Edit{' '}
          <code className="rounded bg-slate-800 px-1.5 py-0.5 text-sm">src/App.tsx</code> to begin.
        </p>
      </section>
    </main>
  );
}
