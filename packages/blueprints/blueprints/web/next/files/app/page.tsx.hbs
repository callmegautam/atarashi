export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <section className="max-w-xl space-y-4 text-center">
        <h1 className="text-4xl font-semibold tracking-tight">{{ project.name }}</h1>
        <p className="text-slate-400">
          Next.js and Tailwind are wired up. Edit{' '}
          <code className="rounded bg-slate-800 px-1.5 py-0.5 text-sm">app/page.tsx</code> to begin.
        </p>
      </section>
    </main>
  );
}
