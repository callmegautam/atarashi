import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    // A new project has no tests yet; that is not a failure.
    passWithNoTests: true,
    include: ['tests/**/*.test.{{#when "provides('language:typescript')"}}ts{{else}}js{{/when}}'],
    coverage: {
      provider: 'v8',
      include: ['src/**'],
      reporter: ['text', 'lcov'],
    },
  },
});
