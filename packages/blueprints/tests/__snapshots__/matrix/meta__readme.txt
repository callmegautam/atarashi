# meta/readme

# dependencies

# files
  README.md  [meta/readme]

────────────────────────────────────────────────────────────────────────
FILE README.md  (644)
────────────────────────────────────────────────────────────────────────
# my-app

Scaffolded with [Atarashi](https://github.com/callmegautam/atarashi).

## Getting started

```sh
pnpm install
pnpm run dev
```


## Scripts

| Script | What it does |
| --- | --- |
| `pnpm run dev` | Run the app with reload on change |
| `pnpm run build` | Compile TypeScript to `dist/` |
| `pnpm run start` | Run the compiled output |
| `pnpm run typecheck` | Typecheck without emitting |


## Blueprints

This project was composed from:

- `core/node-ts` @ 1.0.0
- `meta/readme` @ 1.0.0

`atarashi.json` records that selection. Add another capability with:

```sh
pnpm dlx atarashi add <blueprint>
```
